apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: final
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2   # 👈 ADD THIS LINE
  resources:
    requests:
      storage: 2Gi